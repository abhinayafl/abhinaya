package com.example.ragetbags.witget;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.LinearLayout;

import androidx.annotation.Nullable;

public class icon_bar extends LinearLayout {
    public icon_bar(Context context) {
        super(context);
    }

    public icon_bar(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public icon_bar(Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public icon_bar(Context context, AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
    }
}
